# Python 3.12+
# Install:  pip install -r requirements.txt

fastapi>=0.141.1
uvicorn[standard]>=0.52.1
python-dotenv>=1.2.2

# Active chat provider (LLM_PROVIDER=gemini)
google-genai>=1.0.0

# Other providers — uncomment the one you switch LLM_PROVIDER to.
# Their SDKs are imported lazily, so leaving them out costs nothing.
# anthropic>=0.40.0
# openai>=1.60.0
