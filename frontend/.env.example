# Same OAuth client ID as GOOGLE_OAUTH_CLIENT_ID in backend/.env.
# Public by design — the backend verifies every token, so this value alone
# grants nothing.
VITE_GOOGLE_CLIENT_ID=

# Where the FastAPI backend lives. Leave unset for the default below.
VITE_API_URL=
